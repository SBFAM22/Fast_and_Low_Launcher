using System;
using UnityEngine;
using DummyLoader.Modding;
using static DummyLoader.Modding.VRInput;
using static DummyLoader.Modding.ModUtilities;
using static UnityEngine.Object;

namespace LowModding
{
    public class ModEntry : Entry
    {
        public static GameObject ModdedGameObject { get; set; }

        public override void Load(Mod mod)
        {
            base.Load(mod);
            ModdedGameObject = new GameObject("MODNAME").AddComponent<CustomMod>().gameObject;
            DontDestroyOnLoad(ModdedGameObject);
        }
    }

    public class CustomMod : MonoBehaviour
    {
        //Called When Object Is Created 
        private void Start()
        {
        }

        //Called Every Frame
        private void Update()
        {
        }
    }
}